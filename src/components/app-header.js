import React from 'react';
import './app-header/app-header.css'

const AppHeader = () => {
    return (
        <div className="app-header d-flex">
            <h1>My ToDo</h1>
            <h2>0 more to do, 3 done</h2>
        </div>
    );
};

export default AppHeader;